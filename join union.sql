| Спрашиваешь себя...                    | Используй                       
| -------------------------------------- | ---------------------------
| Нужно только то, что связано?          | `INNER JOIN`                          
| Нужно всё из главной таблицы?          | `LEFT JOIN`                           
| Хочу найти тех, у кого **нет связей**? | `LEFT JOIN ... WHERE правая.id
IS NULL` |


|Тип JOIN| Что делает|Когда использовать|Результат, если нет совпадений |
| --------------- | ----------------------------------------------------
| 🔹 `INNER JOIN` | Показывает только строки, **у которых есть
совпадление** в обеих таблицах | Когда нужны **только связанные** данные
________________________________________________________________________
                        | Строка не попадает в результат      |
| 🔸 `LEFT JOIN`  | Показывает **все строки из левой таблицы**, даже без
совпадений в правой  | Когда нужно увидеть **всех слева**, даже если
справа ничего нет | Показываются строки с `NULL` справа |



| Ключевое слово | Что делает | Пример |
| `UNION` | Объединяет, убирает дубликаты | `SELECT ... UNION SELECT ...`
| `UNION ALL`    | Объединяет всё  | `SELECT ... UNION ALL SELECT ...`
| `LEFT JOIN`    | Показывает всё из левой таблицы, даже без совпадений
справа | `LEFT JOIN B ON A.id = B.id`      |
| `JOIN`  | Строгое совпадение с двух сторон | `JOIN B ON A.id = B.id`       
| `IS NULL`   | Проверка на отсутствие данных  | `WHERE B.id IS NULL`          
| `LIKE`      | Поиск по шаблону         | `LIKE '%word%'`                
| `GROUP BY`  | Группировка по полю       | `GROUP BY product_name`  
| `COUNT(*)`  | Количество строк     | `COUNT(*) AS total`   |


| Хочу...                                  | Используй             |
| ---------------------------------------- | --------------------- |
| Только совпадающие строки                | `INNER JOIN`          |
| Все строки из основной таблицы (слева)   | `LEFT JOIN`           |
| Все строки из вторичной таблицы (справа) | `RIGHT JOIN`          |
| Все возможные комбинации                 | `CROSS JOIN`          |
| Объединить и убрать дубликаты            | `UNION`               |
| Объединить и сохранить повторы           | `UNION ALL`           |
| Узнать, откуда пришла строка             | `'источник' AS label` |
| Строгое совпадение с двух сторон          |  `JOIN`


1 FROM Определяется таблица(-ы), из которой берутся данные.
Если есть JOIN, происходит объединение таблиц.
2 ON (в JOIN) Применяются условия объединения таблиц.
3 WHERE Фильтрация строк (до группировки). Удаляются строки, не
проходящие по условиям.
4 GROUP BY Группировка строк по указанным столбцам.
5 HAVING Фильтрация сгруппированных данных (после GROUP BY).
6 SELECT Выбор нужных столбцов или выражений. Применение функций (SUM(),
AVG(), COUNT() и т.д.).
7 DISTINCT Удаление повторяющихся строк из результата (если указано).
8 ORDER BY Сортировка результата.
9 LIMIT Ограничение количества строк в результате.

Как это запомнить
Если нужно только те строки, где есть совпадения во всех
таблицах — используй INNER JOIN (или просто JOIN).
Для связи таблиц всегда ищи ключи: например, customer_id и order_id.
Если нужна информация из нескольких таблиц — присоединяй их по ключам.
Если хочешь получить только те заказы, по которым есть
инвойсы — обязательно присоединяй invoices через JOIN